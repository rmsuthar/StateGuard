# Integration

## React
Use the `elRef` on your JSX element and only modify it via `safeUpdate`.

## Angular
Export the directive in your template using `#guard="stateGuard"` to access the `update` method.